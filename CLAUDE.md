# Проект: параметрическая косметическая накладка голени (Geometry Nodes)

Параметрическая 3D-печатная косметическая накладка на протез голени —
цилиндрическая оболочка от лодыжки до колена, разрезаемая на переднюю/
заднюю половины и, при необходимости, на верх/низ для печати. Без фото/
3D-сканов — по числовым параметрам (обхваты, длины), средние
анатомические диапазоны взрослого человека. Строится **полностью на
Geometry Nodes** в Blender через headless bpy-скрипты — ручное
моделирование не используется.

Печать: Elegoo Centauri Carbon, филамент PETG. Рабочая зона стола по
умолчанию 256×256×256 мм.

## Жёсткие требования

- Модель полностью параметрическая, на geometry nodes.
- Единицы — сырые **миллиметры** (1 юнит Blender = 1 мм,
  `Масштаб единиц=0.001` в сцене), НЕ метры Blender по умолчанию.
- Целевая версия Blender — **5.1** (не 4.0).
- Толщина стенки — 2-3 периметра соплом 0.4мм PETG (дефолт
  `Толщина_стенки_мм=2.4`).
- Печать сегментируется под стол принтера, линия разреза наклонная (не
  горизонтальная) — менее заметный шов, больше площадь склейки.
- **Никаких пазовых/шиповых соединений деталей и никакой Группы
  «Закладные»** — см. «Что было и почему удалено» ниже. Это не временное
  состояние, а осознанное архитектурное решение.

## Среда сборки/проверки

`blender.org` заблокирован сетевой политикой песочницы, но официальный
PyPI-wheel `bpy` — нет. Рабочая среда: pip-пакет `bpy==5.1.2` в venv
`/opt/bpy51venv` (Python 3.13, cp313-обязательно для этой версии).

```bash
/opt/bpy51venv/bin/python3 scripts/verify_group1.py -- builds/
/opt/bpy51venv/bin/python3 scripts/verify_group2.py -- builds/
/opt/bpy51venv/bin/python3 scripts/verify_group3.py -- builds/
/opt/bpy51venv/bin/python3 scripts/verify_group5.py -- builds/
```

`/usr/bin/blender` 4.0.2 (Ubuntu-пакет) тоже установлен для
обратной совместимости, но 5.1.2 — целевая и основная среда разработки.

**Известные различия API Blender 4.0→5.1** (уже учтены централизованно в
`scripts/nodes_common.py`, НЕ трогать по-файлово при новых доработках):
1. Некоторые `mode`-свойства узлов в 5.1 — MENU-сокеты входа, а не
   атрибуты узла (fallback в `add_node()`).
2. Строки значений enum в MENU-сокетах отличаются от 4.0 (fuzzy-match из
   текста исключения Blender).
3. Type-suffixed идентификаторы сокетов (`Value_Float` и т.п.)
   унифицированы в 5.1 до базовых имён (`_base_identifier()` fallback в
   `in_sock()`/`out_sock()`).
4. `GeometryNodeMeshBoolean.solver` в 5.1 по умолчанию `'FLOAT'`, а не
   `'EXACT'` — форсируется `'EXACT'` централизованно.

Если пишешь новый `build_group*.py` — используй хелперы из
`nodes_common.py` (`add_node`, `in_sock`/`out_sock`, `link`,
`make_frame`), не дублируй эти фиксы локально.

## Архитектура пайплайна

Группа 4 удалена из проекта, нумерация групп в именах node-групп в коде
осталась как есть (`NK.1`..`NK.5`), поэтому реальная цепочка:

```
NK.1_Ось (build_group1_axis.py)
  → Ось_кривая, Точка_A, Точка_B, Факт_длина_дуги_мм, Сторона_ноги
NK.2_Профиль (build_group2_profile.py)
  → Профиль_точки (облако точек N колец × M точек), Точек_в_кольце, Колец_всего
NK.3_Оболочка (build_group3_shell.py; подгруппы NK.3z_Сетка_по_индексам,
               NK.3y_Мост, NK.3a_Половина_оболочки)
  → Перед, Зад (замкнутые watertight половины со стенкой, БЕЗ паза на шве)
NK.5_Печать (build_group5_print.py; подгруппы NK.5a_Разрез_с_крышкой,
             NK.5b_Крышка_среза)
  → Перед_Низ, Перед_Верх, Зад_Низ, Зад_Верх (до 4 STL-сегментов, БЕЗ паза на шве)
```

Каждая группа — отдельная Geometry Nodes группа (node group), которую
можно открыть/проверить/версионировать отдельно.

### Group1 `NK.1_Ось`
`Длина_сегмента_мм` (350, 150–500), `Наклон_сагиттальный_град` (0, ±10),
`Наклон_фронтальный_град` (0, ±10), `Высота_икры_мм_от_колена` (105,
40–250), `Прогиб_икры_мм` (15, 0–40), `Боковой_прогиб_мм` (0, ±30),
`Сторона_ноги` (1=правая/-1=левая), `Разрешение` (48, 8–200).

### Group2 `NK.2_Профиль`
`Ось_кривая`, `Длина_сегмента_мм`, `Сторона_ноги` (из Группы 1),
`Обхват_Точка1_мм` (220), `Точка1_мм_от_колена` (308),
`Обхват_Точка2_мм` (340), `Точка2_мм_от_колена` (105),
`Высота_икры_мм_от_колена` (105), `Икра_выступ_мм` (12),
`Икра_ширина_доля` (0.25), `Гребень_острота_У_лодыжки` (0.85),
`Гребень_острота_У_колена` (0.15), `Гребень_глубина_мм` (6.0),
`Гребень_смещение_град` (8.0), `Овальность_у_лодыжки_мм` (4.0),
`Овальность_протяжённость_доля` (0.25), `Разрешение_кольца` (32, 12–64).

### Group3 `NK.3_Оболочка`
`Профиль_точки`, `Точек_в_кольце`, `Подрезка_снизу_доля` (0, 0–0.35),
`Подрезка_сверху_доля` (0, 0–0.35), `Толщина_стенки_мм` (2.4, 1.2–4.0),
`Расширение_верхней_мм` (3.0, 0–15), `Расширение_протяжённость_доля`
(0.12, 0.02–0.4). Обе половины строятся из одних и тех же
`Внешние_точки`/`Внутренние_точки` — никакого паза/фальца на шве.

### Group5 `NK.5_Печать`
`Перед`, `Зад` (из Группы 3 напрямую), `Точка_A`, `Точка_B`,
`Печать_X/Y/Z_мм` (256/256/256, 150–400), `Зазор_реза_мм` (5.0, 0–15),
`Наклон_шва_град` (12.0, 0–25). `NK.5a_Разрез_с_крышкой` берёт только
`Geometry`, `Точка_на_плоскости`, `Нормаль_плоскости` и сразу
возвращает `Часть_A`/`Часть_B` — без обработки кромки среза.

## Принятые решения (следовать, не переизобретать)

- Толщина стенки = `Внешняя_точка - dir*Толщина` (приближение вдоль
  радиуса кольца), не честная эквидистанта вдоль нормали — для тонкой
  стенки на плавной форме голени разница пренебрежима.
- Замкнутость детали проверяется строго: `Edge Neighbors != 2` → сумма
  граничных рёбер == 0 (`Провер_ВСЕ_OK`), ПЛЮС отдельная проверка знака
  `bmesh.calc_volume(signed=True)` — топологическая замкнутость
  (Edge Neighbors) НЕ гарантирует правильную ориентацию нормалей.
- Boolean-разрезы объёмно пересекающихся тел — только через
  `GeometryNodeMeshBoolean`, никогда `Join Geometry + Merge By Distance`
  (последнее годится только для сшивки НЕ пересекающихся кусков по
  общему шву — так сделаны мосты в Группе 3).
- Крышка произвольного среза (`NK.5b_Крышка_среза`): открытые рёбра →
  `Mesh to Curve` → `Fill Curve` в режиме **NGONS** (не TRIANGLES) —
  иначе внутренний контур полой стенки залился бы сплошняком вместо
  кольца.
- Наклонная (не горизонтальная) плоскость разреза в Группе 5.

## Что было и почему удалено — НЕ ПРЕДЛАГАТЬ СНОВА без нового архитектурного решения

Ранее в проекте существовали: паз/шип (треугольный → трапецевидный,
опционально прерывистый, опционально с ребром жёсткости) между Верх/Низ
в Группе 5; фальц «в четверть» (male язычок / female канал) между
Перед/Зад в Группе 3; отдельная Группа 4 «Закладные» (хомуты 2 планки +
гайки/винты М3, посадочные места под магниты). **Все три удалены
полностью** (история — коммиты вплоть до `7ee29f7`).

**Причина:** все три строились как тонкий профиль, "выдавленный"
(Curve to Mesh) вдоль контура шва на органически изогнутой поверхности,
объединённый/вычтенный через `Mesh Boolean`. Такой подход структурно
несовместим с органической кривизной: там, где реальная кривизна стенки
заметно отклоняется от локального "почти-плоского" приближения профиля
— и особенно там, где контур шва пересекает саму толщину стенки —
Boolean UNION/DIFFERENCE стабильно давал незамкнутые ("дырявые") рёбра.
Это повторялось в каждой итерации (Группа 4: несколько заходов
исправления; Группа 5: прямоугольный → треугольный → трапецевидный/
прерывистый/с ребром, дефекты ~150-900+ незамкнутых рёбер на кейс на
каждой версии). Численный тюнинг (margin/clearance) показывал
**немонотонное** поведение — верный признак архитектурной, а не
параметрической проблемы.

После удаления всех трёх — **все** тестовые кейсы Группы 3 и Группы 5
проходят строгую проверку `Провер_ВСЕ_OK=True` с **0 незамкнутых рёбер**
на каждом выходе. Прямое подтверждение, что пазы были единственным
источником дефектов в проекте.

**Если снова понадобится механическое соединение половин** — не делать
через Boolean-профиль вдоль всего контура шва. Оценить заранее:
(а) отдельные локальные крепёжные элементы простой формы в 1-2
предсказуемо-плоских зонах, а не по всему контуру; (б) чисто внешние
средства крепления (клей, лента, отдельно печатаемые скобы) без
модификации геометрии шва; (в) если всё же нужен паз по контуру —
заранее продумать явный уход от зоны, где контур пересекает толщину
стенки (самое проблемное место).

## Текущее состояние

Пайплайн 1→2→3→5 полностью собран и проверен на Blender 5.1.2:
Группа 1 — 6 кейсов OK, Группа 2 — 9 кейсов OK, Группа 3 — 6 кейсов OK
(0 незамкнутых рёбер), Группа 5 — 4 кейса OK (0 незамкнутых рёбер на
каждом из 4 выходов: Перед_Низ/Верх, Зад_Низ/Верх). `builds/*.blend` и
`renders/*.png` синхронизированы с кодом (кроме архивных
`group3_EXPLODED_TOP.png`/`group5_EXPLODED.png` — иллюстративные,
визуально ещё со старой геометрией шва, не критично).

Половины держатся друг за друга только клеем/посадкой встык —
механической фиксации нет (осознанно, см. выше).

## Возможные следующие шаги

1. Если понадобится соединение/фиксация половин — сначала обсудить
   архитектуру, не повторять Boolean-профиль-по-контуру-шва.
2. Калибровочная печать первого сегмента на реальном принтере — подгонка
   допусков посадки половин.
3. Снятие реальных мерок пациента вместо средних анатомических значений
   (простая подстановка чисел во входные сокеты Группы 1/2).
4. Опционально: полноценный Repeat Zone в Группе 5 для >2 печатных
   сегментов, если накладка длиннее ~360мм по диагонали стола.
